name: flutter_app
description: A new Flutter application.


publish_to: 'none' # Remove this line if you wish to publish to pub.dev


version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  json_annotation: ^0.2.3

  cupertino_icons: ^0.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  json_serializable: ^0.5.4

flutter:
    uses-material-design: true
    assets:
      - assets/questoesSfn.json
      - assets/questoesTaxaJuros.json
      - assets/questoesTcp.json
      - assets/questoesSegurancaNaInternet.json
      - assets/questoesFlutter.json
